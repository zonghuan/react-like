export var isArray = (arr)=>Object.prototype.toString.call(arr)==='[object Array]'

// 虚拟dom的快照
export var domSnap = (dom)=>{
  
}

// 比对虚拟dom
export var diff = (pre,cur)=>{

}

export default {
  root : null
}
